input-view {
  match: Result (this) {
    to-input: FindExit 
    
  }

  message {
    template-macro (T-selectDestination)
  }

  render {
    selection-of (this) {
      where-each (one) {
        layout-macro (L-selectDestination) {
          param (result) {
            expression (one)
          }
        }
      }
    }
  }

  conversation-drivers {
    if (size(this) > 0) {
      conversation-driver {
        template ("첫 번째")
      }
    } 
    if (size(this) > 1) {
      conversation-driver {
        template ("두 번째")
      }
    } 
    if (size(this) > 2) {
      conversation-driver {
        template ("세 번째")
      }
    }
  }
}
