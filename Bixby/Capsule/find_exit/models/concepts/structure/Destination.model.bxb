structure (Destination) {
  description (목적지 이름과 목적지 위치)
  property (destinationName) {
    type (DESTINATIONNAME)
    min (Required) max (One)
  }
  property (destinationLocation) {
    type (DESTINATIONLOCATION)
    min (Required) max (One)
  }
}
