action (appLaunch) {
  description (bixby > youtube)
  collect {
    input (url) {
      type (URL)
      min (Required) max (One)
    }
  }
  type (Search)
  output (URL) {
    evaluate {
      $expr(url)
    }
  }
}
